name: Android APK Builder Workflow

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # 允许在 GitHub Actions 页面手动触发

jobs:
  build:
    name: Build and Package APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Initialize Environment Variables
        working-directory: ./netshield-vpn  # 指示在此目录下执行
        run: |
          # 直接创建一个空的 .env 文件以满足依赖
          touch .env

      - name: Grant execute permission for gradlew
        working-directory: ./netshield-vpn  # 指示在此目录下执行
        run: chmod +x gradlew

      - name: Build Debug APK
        working-directory: ./netshield-vpn  # 指示在此目录下执行
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: netshield-vpn-debug-apk
          # 注意：这里的打包路径也需要加上子目录前缀
          path: netshield-vpn/app/build/outputs/apk/debug/*.apk
          retention-days: 7

      # 提示：如果需要打包 Release 版本，需要配置签名密钥
      # - name: Build Release APK
      #   env:
      #     KEYSTORE_PATH: ${{ secrets.KEYSTORE_PATH }}
      #     STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
      #     KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      #   run: ./gradlew assembleRelease --no-daemon
      #
      # - name: Upload Release APK Artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: netshield-vpn-release-apk
      #     path: app/build/outputs/apk/release/*.apk
